version: '2'

services:
  app:
    ports:
      - '3000:3000'
    volumes:
      - '.:/app'
    env_file:
      - '.env.development'

  sidekiq:
    env_file:
      - '.env.development'

  cable:
    env_file:
      - '.env.development'
