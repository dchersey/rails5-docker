version: '2'

services:
  app:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - '.:/app'
    env_file:
      - '.env.development'

  sidekiq:
    build: .
    env_file:
      - '.env.development'

  cable:
    build: .
    env_file:
      - '.env.development'
